import {useEffect,useState} from "react";import {io} from "socket.io-client";const API="https://smart-health-portal-backend-production.up.railway.app";export default function OPQueue(){const u=JSON.parse(localStorage.getItem("user")||"null");const [q,setQ]=useState(null),[current,setCurrent]=useState(0),[waiting,setWaiting]=useState(0);const load=async()=>{const r=await fetch(`${API}/api/op-queue/${u.id}`);const d=await r.json();if(d.success){setQ(d.queue);setCurrent(d.current_token);setWaiting(d.people_waiting);}};useEffect(()=>{load();const s=io(API);s.on("queueUpdated",load);return()=>s.disconnect();},[]);const join=async()=>{await fetch(`${API}/api/op-queue/join`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({user_id:u.id})});load();};const leave=async()=>{if(q)await fetch(`${API}/api/op-queue/${q.id}`,{method:"DELETE"});load();};return <div className="min-h-screen bg-blue-50 py-12 px-6"><div className="max-w-5xl mx-auto"><div className="text-center mb-10"><p className="text-blue-600 font-semibold">SMART HOSPITAL SERVICE</p><h1 className="text-4xl font-bold text-gray-800 mt-2">OP Queue Management</h1><p className="text-gray-600 mt-3">Join the outpatient queue and monitor your token.</p></div><div className="grid md:grid-cols-3 gap-6"><Card title="Current Token" value={`#${current||"—"}`}/><Card title="People Waiting" value={waiting}/><Card title="My Token" value={q?`#${q.token_number}`:"Not joined"}/></div><div className="bg-white rounded-2xl shadow-lg p-10 text-center mt-8">{q?<><h2 className="text-3xl font-bold">You are in the queue</h2><p className="text-5xl font-bold text-blue-600 mt-5">Token #{q.token_number}</p><button onClick={leave} className="mt-6 bg-red-500 text-white px-6 py-3 rounded-lg">Leave Queue</button></>:<><h2 className="text-3xl font-bold">Join OP Queue</h2><button onClick={join} className="mt-6 bg-blue-600 text-white px-7 py-3 rounded-lg font-semibold">Get My Token</button></>}</div></div></div>}function Card({title,value}){return <div className="bg-white rounded-2xl shadow-lg p-8 text-center"><p className="text-gray-500">{title}</p><p className="text-4xl font-bold text-blue-600 mt-3">{value}</p></div>}
